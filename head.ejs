<!doctype html>
<html lang="<%= lang %>" dir="<%= dir %>">
<head>
  <meta charset="utf-8" />
  <!-- Mobile-first: prevent iOS Safari "desktop" scaling/whitespace and support notch safe-area -->
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title><%= title %> | G-Zone Cafe</title>
  <meta name="theme-color" content="#1b8a54" />
  <!-- bump version to avoid stale CSS cache on mobile -->
  <link rel="stylesheet" href="/css/styles.css?v=2417" />
</head>
<body>
